<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loginmypham">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoginMyPham">
        <activity
            android:name=".Activit.Activity.Success"
            android:exported="false" />
        <activity
            android:name=".Activit.Activity.Otp"
            android:exported="false" />
        <activity
            android:name=".Activit.Activity.Register"
            android:exported="false" />

        <activity android:name=".Activit.Activity.MainActivity"
            android:exported="false"/>

        <activity
            android:name=".Activit.Activity.Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>